#!/usr/bin/env bash
# filename: config-env.sh
# purpose: Define environment variables for use in these user configuration
#   setup scripts.

# For example, something like this:
# (Any conflicts with reserved variable names or otherwise pre-existing vars?)
#CONFIG_ROOT="$HOME"
#CONFIG_FOLDER=".config-files"
#CONFIG_LOCATION="$HOME/$CONFIG_FOLDER"

